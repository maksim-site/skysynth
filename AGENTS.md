# Prototype Instructions

Run the local server yourself and open the preview in the browser available to this environment. Do not give the user server-start instructions when you can run it.

Before making substantial visual changes, use the Product Design plugin's `get-context` skill when the visual source is unclear or no longer matches the current goal. When the user gives durable prototype-specific design feedback, preferences, or decisions, record them in `AGENTS.md`.

When implementing from a selected generated mock, treat that image as the source of truth for layout, component anatomy, density, spacing, color, typography, visible content, and hierarchy.

Build app UI in `src/`. Keep `.openai/hosting.json`, `worker/index.js`, `scripts/prepare-sites-build.mjs`, and `tests/sites-worker.test.mjs` intact so the same local prototype can be handed to Sites. Before a Sites handoff, run `npm run build` and `npm run test:sites`; the build must leave `dist/client/index.html`, `dist/server/index.js`, and `dist/.openai/hosting.json`.

## Project design lock

### Latest corrections, 08.09.2026 (supersede older responsive/motion notes)

- Latest mobile framing correction: the user rejected shrinking the entire scene to fit Safari. Restore the broad, substantial v1 composition: the complete portrait photo reaches both edges, the fixture may continue behind the header and heading, and only its lower room fades. Size the scene by phone width and raise it independently of the reading budget; retain the complete interactive PCB. Keep the CTA clear through compact copy spacing on short phones and safe-area bottom padding. Preserve all other approved changes and publish directly to the existing GitHub v2.
- iPhone Safari follow-up: keep the hero CTA above the expanded bottom address bar, with safe-area clearance and natural scrolling on very short screens. Do not shrink the photograph into a narrow card to achieve this; the later v1-framing correction above supersedes that rejected approach.
- Service disclosure follow-up: smoothly open and close both “Что входит в услугу” and “Как проходит работа”. Preserve native details/summary semantics, reverse from the current height on repeated taps, and settle immediately for keyboard/reduced motion and when crossing the desktop breakpoint. Preserve the shared fixture clearance.
- Startup correction: prepare the principal 3D board immediately so it can open promptly, but never hold the logo splash for 3D JavaScript, GLBs or the pendant. Restore the first-screen-only image/font wait and original 2200 ms desktop / 3600 ms phone watchdog. Preserve a complete static PCB/fixture while the interactive module loads. Allow a late pendant to fade in without restarting its swing; this supersedes the previous eight-second wait and disabling a late lamp. Background neighbours and alternate-theme images start after the page is revealed.
- Post-release follow-up: keep the privacy-page logo on the left and move the home link out of the header into the reading column above the policy heading. The user explicitly authorized immediate publication to the existing v2. Prioritize the first 3D model over background downloads; preserve the seven exact models and their progressive GPU preparation.
- Release authorization, 08.09.2026: the user approved a final site/client-feedback audit and publication of the next numbered version to GitHub. The existing public root is v1 at `2cb1635`; publish the accepted current source at `/skysynth/v2/` on GitHub `main`, retaining the old root through the pinned release in `releases/pages.json`. No real form submission or GitVerse/Sites deployment is authorized by this request. This supersedes earlier local-only notes for this release.
- Latest CTA follow-up at 681×849: apply the attached-to-description button spacing throughout the stacked hero, including 540–720px and taller tablet windows. Remove the shared automatic top margin, not just its 721–960px short-height override. Preserve the phone's existing 20px description-to-button gap and accepted compact spacing at 950×850.
- Latest spacing follow-up at 781×850: keep the hero CTA close to the description rather than pinned separately to the viewport bottom. Use spare height for more breathing room above the heading and between heading/description; taper those gaps on tighter stacked viewports so the accepted 950×850 fit remains intact. Other approved proportions stay unchanged.
- Latest first-screen follow-up at 950×850: keep the full hero heading, description and CTA visible within this viewport. Let the centred, left-aligned reading column widen progressively above 720px and compact its vertical gaps at tablet heights; preserve readable font sizes, the complete scene/PCB geometry and the restored theme wave. Shorter viewports may scroll naturally.
- Latest follow-up at 857×850: the photo dissolved around the text still looks cut out. Extend the accepted complete-scene-above/left-aligned-copy-below composition through 960px, and remove the temporary local white veil. Keep the source room and mounting plate complete. The user also reports pendant stutter when changing themes and explicitly wants to retain the accepted theme wave: isolate the moving lamp in a named transition layer with a short old/new crossfade, keeping the header above it. Do not replace the wave with a plain page fade or restart/pause the pendulum to hide the issue.
- Latest hero readability follow-up at 857×850: keep the light-theme room visible above the text, but quiet the detailed photograph locally underneath the reading column with a soft edge. Wide photographs must reach the upper viewport edge, without the former 0–32px offset or top fade. Preserve proportional scene sizing, the independent PCB and the accepted stacked/phone layouts. This supersedes earlier wide-photo ceiling-offset/top-fade notes.
- Latest hero follow-up at 711/720×852: centre the 540–720px reading column but left-align its lines and CTA. Use the existing wide room photograph through this range, retaining its side continuation and complete mounting plate. Extend the animated pendant cord to the hero's upper edge and softly fade its separate illumination before the copy; no hard clipped light boundary. Preserve the phone composition below 540px and the wider desktop layout. This supersedes the centred-line and portrait-photo requirements for 540–720px below.
- Final-review static-page correction: let the privacy heading hyphenate/wrap on narrow screens, including 320px; preserve its wording and wider typography. No legal or analytics configuration change was requested.
- Final-review correction at 683x843: reserve responsive vertical clearance for the entire shared services fixture below 740px. Both closed and expanded native disclosures must remain above/below the PCB without overlap. Keep the scene at its approved scale; replace the fixed 150px row with a width-dependent 150–266px track, not a cropped/rebuilt image. The user authorized a full local responsive/functionality audit, still without publication or real submission.
- Final-review interaction fixes: hide the long active hero gesture label across the entire stacked layout through 720px. The active hero Canvas owns touch gestures at every width, while the mouse wheel and surrounding page remain scrollable. Escape closes the hero inspection and restores focus to its trigger without moving the page.
- Latest phone follow-up: the user now requests the same tap-to-lift, freely finger-rotatable hero PCB on phones under 540px. Preserve the accepted large static resting pose, exact fixture, compact header, left-aligned copy and hidden pendant. Retain the prepared Canvas after return; scroll normally outside active orbit, and reserve a compact gesture hint below the fixture. This supersedes the static-only phone restriction below.
- Latest phone gallery follow-up: replace the seven line indicators with the existing real-board thumbnails and numbers, matching desktop. Keep at least 44px tap targets; fit all seven where possible and let only the row scroll on narrower phones. Dissolve the table photograph at its actual lower edge/visible crop into the following section; never mask or blur the independent 3D PCB or controls. These instructions supersede the line-indicator requirement below.
- Same-turn hero clarification: raise only the phone PCB by 12px relative to the unchanged rear fixture/photo, including its live Canvas and static fallback. Do not lift the whole stand. Hide the long active-3D gesture hint on narrow phones; retain only the small invitation before opening.
- Same-turn materials follow-up: the Result / project-materials rows must stay stationary under hover. Do not change padding, grid gaps, size or position on hover; a quiet rule-colour change is sufficient. The old shared `.deliverable, .deliverable:hover` geometry overrode responsive padding/gaps and caused jitter; keep geometry on the base selector only.
- Same-turn dark hero feedback: remove the pale grey haze from the principal PCB in the dark theme. Match the existing light theme's crisp black mask and component contrast on the image, including the phone fallback. Preserve the approved light theme, exact assets and scene geometry.
- Latest dark-hero refinement: the full light-theme contrast transfer was too strong. The user asked to return roughly halfway toward the previous rendering; use the softer dark-only `.78` brightness / `1.35` contrast / `.96` saturation correction, preserving light mode and 3D lighting.
- The user explicitly selected the existing public `https://maksim-site.github.io/skysynth/` as the mobile reference. Below 540px restore its substantial static PCB/fixture, compact 262px header and bottom-aligned copy/CTA with a 42px safe inset. Short phones scroll naturally. Fade only the photograph's empty side/lower margins, never the independent PCB. Preserve the current wider composition; show the accepted pendant from 540px where it fits, never on narrower phones. Full navigation still starts at 32.5em.
- On phones through 620px, use the published gallery's whole-stand proportions, title/info/count above the model, large arrows, and seven restrained line indicators below the stand with 44px hit areas. Keep the current seven exact GLBs, corrected black rear mask, lighting, free orbit and canonical landing. Desktop retains the approved thumbnail selector. This supersedes the earlier requirement for thumbnail images on phones.
- Services remain peer left/right descriptions around the existing shared decorative board. From 740px align their icon, heading, description and detail-row starts with shared grid tracks; retain all approved copy and mobile native disclosures.
- Hero photo-to-3D is one quick 0.48s lift: the color/image crossfade begins immediately, not after a lift/pause. Prepare and retain the actual hero Canvas before interaction. Return from any inspection angle along a smooth spherical camera path, then crossfade into the photograph while the descent continues; do not add a freeze or a straight camera cut through the PCB.
- Gallery readiness means shader compilation and completed upload renders in the actual Canvas, not GLB download alone. Warm one new model at a time outside visible transitions; keep prepared React/GPU instances, but detach inactive CAD trees from the active render graph. Their local transforms are static; only the presentation groups animate. Never remount the Canvas or recreate a model at the visible hand-off.
- Anchor navigation must reveal destination text immediately, including repeat clicks. Normal scroll reveals are early, short and never begin fully invisible/blurred. Do not re-hide already visited content.
- The four-board panorama's upper fade must end in the empty laboratory above the highest component, calculated from the real image crop. All boards/cradles remain sharp; no image blur or replacement asset.
- Implementation and QA remain local only. Public reference reading is authorized; commit, push, publication and real form submission are not.
- Further screenshots, 08.09.2026: preserve comparable physical clearance around the services PCB at 1148px and 1183px. Use one continuous 1 / 1.12 / 1 grid from 740px, not the former narrower middle track below 1180px. At 540–720px centre the hero headline, description and CTA beneath the lamp/fixture. This range is interactive: show the click-to-3D hint and preserve compact render quality. Only under 540px keep the published static, left-aligned phone hero without a misleading 3D hint.

### Preserved baseline and historical decisions

- Visual direction: variant **C «Стенд»**, chosen 18.08.2026 from the preview set in `скс/previews`. Current structure is hero, services, reverse engineering, developments, project materials, FAQ and request.
- Dark is the default theme; light is a complete alternate behind the header icon with the same layout and component grammar.
- Type: `Unbounded` (300/400/500) for display, `Golos Text` (400/500/600) for body. Both are self-hosted in `public/assets/fonts` with cyrillic + latin subsets. Do not add a webfont CDN.
- Palette: `--bg #08090a`, `--deep #13171c`, `--fg #f0ece4`, gold `#c9a227` / `#e0c877`, hairline borders only.
- No frosted glass, no backdrop blur, no ambient gradient wash behind sections, no decorative accent rules that animate. The client rejected all of these.
- The header is a floating capsule centred at the top, `position: fixed`. Section `scroll-margin-top` must clear it.
- Services are a numbered list, not cards: round icon, number, title, then text with small tags. The lead service block is not boxed. Tags may only restate what the service text already says, never add a new claim.
- `Разработки` is a two-column block: capsule chips with round board thumbnails on the left (same shape language as the header capsule), the 3D stage on the right, the description under both.
- The hero copy is capped to `min(52%, 660px)` and the composite stage is nudged right on wide screens so text never overlaps the board. `.hero-copy` is `pointer-events: none` with `pointer-events: auto` on its children, so the board stays clickable next to the text.
- The hero veil is two gradients: a horizontal one that holds the copy column and a vertical one that seats the scene. Both have a light-theme counterpart.
- Never stretch or fill-crop electronics. Keep the complete board inside a `contain` treatment with breathing room.
- AI laboratory imagery may be used only as a non-evidence backdrop. Boards shown on top of it must remain the client's exact 3D/CAD renders and stay fully visible.
- Do not add real production photos back; the client rejected them for this site.
- Motion stays restrained: hero copy entrance, one-shot reveal on scroll, hero board lift into 3D, board cross-fade, and the process trace drawing itself once. Everything respects `prefers-reduced-motion`.
- Do not show a fixed five-stage «Процесс»: the client source lists work types but does not define a five-step sequence. The section was removed on 24.08.2026 together with its navigation link.
- Any `<img>` sized by `aspect-ratio` also needs `height: auto`: the HTML `height` attribute is a presentational hint and otherwise wins.
- Section backgrounds alternate `--bg` / `--deep` and each section carries its own soft radial light via `::before`, with a fading hairline divider in `::after`. After `Разработки`, the separate materials-section rule is removed; retain only the gallery photograph's neutral near-black dissolve so there is no white line, hard crop or blue-grey fog.
- `--bg` and `--deep` must stay visibly different: the sections alternate between them, and when the two were nearly identical the whole page read as one black mass.
- Ground all visible copy in `скс.docx`, and prefer the client's own wording over a paraphrase. The hero description is their company description, trimmed. Do not add clients, figures, specifications, certifications or capabilities that are absent from the supplied material.
- Keep visible Russian copy plain and natural. Avoid promotional filler and em dashes.
- The intake form is intentionally simple: name, company, phone/email and a short message. Do not add a technical-brief upload; the client asked for briefs and files by email.

## Client answers, 17.08.2026

- Boards are examples of competence, not products for sale. Say so on the page.
- Main service: разработка программно-аппаратных комплексов. Additional service: реверс-инжиниринг. The page hierarchy must reflect that split.
- All boards and 3D models may be shown, except the `AKYMA RX 433 mHz` receiver (client: "убрать акума"). It is excluded from `boards` in `src/App.jsx` and must stay out.
- Enquiries go to `info@skysinth.com`. Technical briefs and files go to the same address by email.
- Logo received: `public/brand/` holds the final SKS wordmark, S mark and full favicon set. Header and footer both use the full theme-matched wordmark, not the single-letter mark, and `index.html` links the whole favicon set.

## Client feedback, 03.09.2026

- Use `БАС` instead of `БПЛА` in all visible site copy and metadata.
- Present reverse engineering explicitly as the second type of company service. The complete accepted lamp scene remains conceptual and must not imply that its pictured PCB is a reverse-engineering case.
- More freely rotatable boards are welcome only when they come from supplied, permitted source models and do not compromise performance. `AKYMA RX 433 mHz` remains excluded.
- Match the solder mask on both faces of every interactive controller to the supplied `IMG_7368.JPG` reference: opaque black on the top and bottom. Preserve the exact board geometry and other materials.
- Images across the app and static pages must not start the browser's native image/file drag. Keep them non-draggable while preserving links, the hero click-to-3D threshold and all WebGL orbit interactions.
- Services follow-up, 05.09.2026: `Вид услуг 01/02` is rejected. Development and reverse engineering belong together under `Услуги`, with peer headings, meaningful outline icons and the same description/detail treatment. Preserve the existing factual copy and keep the six development disciplines clearly subordinate. A separate photograph after the services was rejected: the complete accepted lit-board scene must blend into the services themselves, between development on the left and reverse engineering on the right. Keep the centre board and light clear, dissolve the surrounding room into the section, and never imply that this PCB is a reverse-engineering case. On narrow screens integrate a smaller complete scene between the two descriptions. No crop, regeneration, service caption or duplicate standalone image. Keep `#reverse` pointing to the actual service description. The accepted main-hero pendant swing stays unchanged.

- Gallery controls follow-up, 05.09.2026: keep the board title high above the model. The separate numbered button row at the upper right was rejected; integrate the seven actual-board thumbnails with their numbers below the stand, with the label `Выбрать разработку` and a restrained current-board underline. Keep the touch-friendly choices below the stand on phones too. Preserve exact scene framing, model sizes, transitions and free orbit.
- Source audit follow-up, 05.09.2026: the user selected `Кратко, строго по источнику`. Summarize the development scope from `скс.docx`, retaining production, tests, quality control, support, documentation and component-library work. Do not claim the development copy is verbatim. The subsequent reverse-engineering correction below supersedes the earlier removal of its description and details.
- Reverse copy restoration, latest 05.09.2026: the user confirmed that the previous GitHub-published wording had already been accepted by the client and rejected the empty reverse-engineering block. Restore its description and the exact `На входе / Что делаем / На выходе` text from the published `2cb1635` baseline, including the component-replacement wording. Treat that user-confirmed copy as an additional approved source; do not strip it again merely because it is absent from `скс.docx`. Keep development on the left, reverse engineering on the right, and the shared decorative PCB scene between them. Do not reintroduce a separate reverse-only photo section or change gallery behavior.
- Services seam follow-up, 05.09.2026: remove the old gold radial glow and boundary rule from the services section, and reduce the unnecessary space before the shared photograph. Preserve the photograph's own inspection-light pool and the accepted hero lamp.
- Gallery lighting follow-up, 05.09.2026: keep the original front key/rim lights but add a warm-neutral shadow-free rear fill and rear environment reflection for free-orbit gallery models. Both board faces must be readable without changing the black solder mask, other materials, GLBs, geometry, camera scale, hero lighting or transition mechanics.
- Gallery landing follow-up, 05.09.2026: every incoming board must land in the approved front-facing pose even after arbitrary manual inspection or a hot reload. Pin the controls' home to BoardCanvas's explicit camera distance and origin target; flush residual damping only at the hidden model swap. Never reset the outgoing board early, capture a temporary inspection pose as home, remount the Canvas per board, change board scale or remove free orbit. Regression tests live in `tests/board-presentation.test.mjs`.

## Hero background experiment, 05.09.2026

- Latest responsive correction, 06.09.2026: the 1180px asset swap and additional aspect-ratio layout switches caused rejected jumps. Use the same wide photograph/normalized fixture above 720px, scaling continuously. A shorter photograph sits toward the ceiling and dissolves at its own top/bottom edges, not only the viewport edges. Below/equal 720px use a compact scene above the copy so the full heading is visible on the first screen. On 540–720px put the pendant on the left and progressively shift the complete fixture/PCB right; 544px and 560px are explicitly requested to include it. Only widths under 540px omit it. This supersedes the older all-phone pendant and portrait/aspect-ratio rules below. The three navigation links fit from 32.5em, including 544px; use the compact content-sized capsule and reserve the dropdown for narrower screens. Do not move the PCB independently from its fixture.
- Latest performance feedback, 06.09.2026: the user reported a visibly choppy pendant and asked for verification in the in-app browser, without subagents or treating a design skill as visual authority. Keep the accepted ±3.6°/10s swing and both lamp assets, but composite lamp, beam and textured haze as one smaller transformed layer. No separately animated mist or per-frame canvas generation. Pause offscreen/hidden and under 540px; respect reduced motion. Verify after a full reload as well as resizing, because a hot-reloaded page can retain previous observer closures. Do not claim the user's intermittent low-FPS condition is reproduced merely from a successful short benchmark.
- Intermediate header/composition follow-up, latest 06.09.2026: the stretched capsule with a hamburger around 848px is rejected because all three links fit. Show the normal navigation in a content-sized capsule from 35em; reserve the dropdown for genuinely narrow widths and close it when crossing that breakpoint. Do not couple the navigation breakpoint to the hero asset/layout breakpoint. In the intermediate two-column hero, move the pendant left into the gap above the copy and lower the complete copy group slightly. Preserve the fixture/PCB geometry, phone portrait composition, wide hero and both themes.
- Hero spacing follow-up, latest 06.09.2026: in narrow in-app windows the pendant must not almost touch the fixture, including at both swing extremes. Reserve at least 22px separation; on phones place the complete fixture and PCB below the lamp, and shift the complete frame right where there is space beside it. Keep the board large. Static phone and interactive portrait scenes share `heroLayout.js` through `useHeroStageLayout.js`. Fade the photograph at its actual visible crop, not outside the clip; allow the light/haze to spill beyond the visual column without a hard vertical boundary. Never mask the PCB, independently reposition it against its fixture, alter the approved wide hero or change 3D controls to solve spacing.
- Responsive correction, latest 06.09.2026: the small contained phone hero and missing pendant are rejected. Fit the physical mounting fixture, not all empty margins of the source photo; keep the exact PCB large and fully visible. The swinging pendant must remain visible on phones and intermediate viewports in both themes. Use content-driven hero height and a separate readable copy area, never force all content into a short viewport by shrinking hardware. Intermediate landscape windows use two columns only where both objects and copy fit.
- White pendant correction, latest 06.09.2026: the first white recolor was rejected for visibly poor cutout edges. Use the independently generated genuine-alpha `pendant-white-v2.webp`; do not apply the old dark lamp's luminance mask to it. Preserve the accepted dark lamp and motion. Predecode both themes before the initial loader exits.
- Mobile services correction, latest 06.09.2026: the standalone photograph between long service articles is rejected. The shared scene is a short dissolving visual bridge within the service composition, with no hard rectangular room; keep all approved copy in accessible native disclosures on phones. Tablet services retain the shared scene between peer columns, not above them. Desktop retains full expanded service text. Native disclosures may compact the mobile reading flow, not remove approved descriptions.

- Responsive follow-up, latest 05.09.2026: preserve the agreed wide desktop world and all approved copy, but support intermediate windows as deliberately as phones. `src/responsive.css` is the final responsive layer. Through 1100 px, and in portrait/square windows through 1366 px, the hero uses a contained complete portrait fixture in its own area, above the copy in portrait and beside it at a 6:5 or wider aspect ratio. Phones keep the lightweight static composite; other narrow layouts keep click-to-3D. Never cover-fit the portrait fixture across the entire hero or place it behind the reading area again. `heroLayout.js` shares the picture/preload query and tests its aspect-preserving fit. The pendant belongs to the wide desktop composition, with the existing loading/readiness behavior.
- Responsive services: keep the existing three-part desktop composition above 1100 px. At 700-1100 px, show one small complete decorative scene above two peer service columns; below 700 px keep development, shared scene, reverse in reading order. All restored reverse-engineering wording remains unchanged. Do not add a duplicate or separate photo section.
- Responsive gallery/form: use a compact stage on tablets/phones, preserving the photo/video shared coordinate frame, exact models, camera home and spin timing. Controls remain below the stand; below 360 px the 44 px minimum-width thumbnails may scroll horizontally, and selection must reveal the active thumbnail without moving the page. Info controls are touch-sized, long descriptions may scroll, and form inputs remain at least 16 px. At short heights allow content scrolling instead of clipping text or shrinking the board independently. Initial hash links must resolve after the loader has mounted their targets.

- The new lamp belongs to the main top hero, above the left copy, not the developments gallery or the accepted reverse-engineering scene.
- Use a simple broad dark pendant shade on one thin, nearly invisible cord. Mechanical desk-lamp arms, hinges and springs were rejected.
- Keep the room dark and the illumination subdued. The current experiment is gentle pendulum movement with a matching moving light pool; keep the headline clear and the exact client board independent.
- The user accepted the pendant experiment and requested local main-hero integration. Set the reviewed light strength near 80% rather than 58%, darken the background slightly, and let restrained illuminated haze pass over the copy area without reducing readability. No publication is authorized.
- Main-hero 3D now allows an unrestricted horizontal orbit and almost pole-to-pole vertical inspection, like the gallery. Keep the exact geometry, lighting, photo-to-3D transition, click-to-return and wheel-to-scroll behavior. A drag returning to its initial pointer position must never be treated as a click.
- Prepare lamp/mist bitmaps once and animate only compositor transforms/opacity. Pause the lamp and hero 3D idle motion when the first screen is offscreen or the tab is hidden. Respect reduced motion.
- Swing follow-up, 05.09.2026: make the accepted pendulum a little more noticeable, without speeding it up. Use a ±3.6° swing (previously ±2.65°) with the same smooth 10 s cycle; keep the light and haze attached to the lamp and leave their brightness unchanged.
- Light pendant follow-up, latest 05.09.2026: show the same photographed pendant in both themes wherever the wide hero is used. Preserve the shared geometry, ±3.6°/10 s motion and cached bitmap layers across theme switches. In light mode only, lift the dark shade's detail and use subdued warm-white beam, mist and reflections suited to the neutral laboratory. Do not remount or restart the lamp when toggling themes. Both themes must prepare/decode the lamp under the initial loader, with the same existing readiness gate and watchdog. Keep the narrow-layout rules unchanged.
- Theme transition, latest 05.09.2026: switch themes through a soft light wave originating at the sun/moon button, with a restrained highlight/shade frontier for depth. Use native view-transition snapshots and a feathered radial reveal, without page zoom, text motion, DOM clones or a new animation dependency. Keep the current lamp instance and its animation phase. Warm destination assets first, block overlapping switches, clean up transient layers, and retain a short fade fallback plus a non-spatial reduced-motion path. The effect must work at the current scroll position as well as at the hero.
- Loader follow-up, 05.09.2026: prepare and decode the pendant, beam and haze while the SKS loader is visible, and commit their final layout before revealing the hero. Never mount the lamp only after `siteReady`, change the scene framing after reveal, or change the viewport width when the scroll lock is removed. Keep the 620 ms minimum; both desktop themes' complete first-screen scene has an 8 s failure watchdog, with the original stable hero as fallback rather than a late lamp pop-in. Other loader limits stay unchanged.

## Implementation notes

- The board in `Разработки` is live 3D by default. The first GLB and lazy 3D chunk warm immediately after the critical page assets, and the remaining GLBs are staggered during idle time. This prevents a still-image flash while avoiding a simultaneous mobile decode spike.
- `OrbitControls` runs with `enableZoom={false}` so the wheel scrolls the page instead of zooming the board. Keep `overflow: clip` (never `hidden`) on `.board-stage` and `touch-action: auto` on the canvas: both would otherwise swallow the wheel.
- The still render is hidden as soon as the model is ready, so the board never doubles up.
- In the hero, a short click on the 3D board (moved under 6 px, held under 400 ms) returns it to the still render. Dragging to rotate must not close it. There is no close button any more; the gesture hint states both actions.
- Switching boards preserves the outgoing manual pose until the edge-on blurred hand-off. `BoardScene` resets the camera only under that hidden swap, then lands the incoming board at the canonical presentation angle. Do not remount the Canvas per board; that rebuilds the WebGL context.
- The board drifts slowly on its own and keeps the restrained levitation while the pointer is over the stage. After manual interaction or a transition, the idle motion gains amplitude smoothly instead of snapping back. `BoardGallery` mirrors the visible-stage state to `data-drift`; reduced motion disables the drift.
- `src/media.js` is deliberately free of three.js. `src/BoardCanvas.jsx` is the only lazy entry point into `@react-three/*`, so the 3D stack (about 285 kB gzipped) downloads on hover or click, not on first paint. Do not import `boardScene.jsx`, `Canvas` or `useGLTF` from `App.jsx`, `HeroBoardExperience.jsx` or `BoardGallery.jsx`.
- Surfaces are flat: `--bg` / `--deep` / `--raised` plus hairline `--line` borders. There is no panel component and no blur. The only soft light comes from the per-section `::before` gradients and `.stage-glow` behind the board.
- `FORM_ENDPOINT` is intentionally empty: the client will wire real sending when the site goes on a server. The form opens a prefilled email to `info@skysinth.com` meanwhile. A consent checkbox is required and links to `public/privacy.html`.
- The policy is called «Политика конфиденциальности» everywhere, matching the client's other sites. Do not rename it back to «Обработка персональных данных».
- The cookie bar offers a real choice, «Принять» / «Отклонить», stored in `localStorage` under `sks-cookie` as `accepted` / `declined`. The statistics counter only initialises after «Принять»; a single «Понятно» button was rejected on 20.08.2026.
- Cookie wording feedback, 23.08.2026: keep the bar compact and conventional: one short sentence without naming Яндекс.Метрика, a short «Подробнее» link, and the two actions «Отклонить» / «Принять». Detailed analytics wording belongs on the linked policy page.
- `METRIKA_ID` in `src/App.jsx` is empty, so no counter loads. Fill in the Yandex.Metrika number to switch it on; `useMetrika` handles the rest and the form reports a `form_sent` goal.
- Static pages live in `public/`: `privacy.html`, `404.html`, `robots.txt`, `sitemap.xml`. They carry their own copy of the font faces and a small stylesheet, so they do not depend on the app bundle. Canonical host in those files and in the JSON-LD is `https://skysinth.com`; change it everywhere if the domain differs.
- Smooth scrolling is deliberately off everywhere (CSS, logo click, board list). The client asked for instant jumps.
- No active-section highlight in the nav and no scroll progress bar: both were tried on 19.08.2026 and the client removed them.
- Anchor navigation scrolls smoothly (`scroll-behavior: smooth` on `html`, plus a smooth `scrollTo` on the logo). Plain jumping was rejected on 20.08.2026. Inertial/Lenis-style scrolling is still out.
- Reverse-engineering wording from the published `2cb1635` baseline was confirmed by the user as client-approved on 05.09.2026; retain that exact scope unless the user requests a copy change.
- Visual feedback, 21.08.2026: do not use the phone snapshots from `public/assets/images/production/` as visible site photography. Use the clean theme-matched studio concept renders instead and label them as conceptual visualizations.
- Keep the header wordmark non-selectable and non-draggable. The hero board itself opens 3D; do not add a second invitation button. Use only a small explanatory note at the bottom of the stage.
- Services must use the full editorial width without a dead right edge. The process reads in sequence without a click selector and keeps every board fully visible. FAQ answers open and close with restrained motion. The light theme is a muted laboratory-grey alternate, not a bright ivory page.
- Services feedback, 23.08.2026: keep the section technically credible but compact. The lead service remains dominant, repeated tags are unnecessary, and supporting services should read as a light numbered engineering register rather than a wall of cards or long text.
- Services wording correction, 24.08.2026: preserve the main-service hierarchy in the layout, but use neutral visible labels such as `Устройство целиком`, `Направления работ` and `Работа по готовому изделию`; do not present them as promotional `основная`, `отдельная` or `дополнительная` offers.
- Motion feedback, 24.08.2026: section reveals should inherit the soft Lider feel: a short upward move with a small blur resolving through a smooth ease, never a hard pop. Reduced motion still removes the effect.
- Mobile feedback, 24.08.2026: use a three-line hamburger rather than a text «Меню» pill. The hero board is a subdued static layer inside the first screen, behind the copy, not a separate interactive 3D block. In the gallery keep the whole board visible, reduce the empty top gap, enlarge arrow hit areas, and keep the board title off the physical stand.
- Mobile correction, later 24.08.2026: remove the hero board/background layer entirely on phones and keep a clean first screen; remove «Инженерная команда · Санкт-Петербург». Narrow the floating header capsule, show the complete gallery table and four-board panorama, and contain the reverse-engineering scene so its lit PCB does not protrude between text blocks.
- Gallery transition correction, 24.08.2026: a manually rotated board must never rush toward the camera, jump in scale, or inherit an arbitrary inspection angle after an arrow press. Return the camera along a constant-radius orbit, keep the live GLB scale fixed, and avoid the baked transition video on phones where its cover crop changes the apparent table scale.
- Performance feedback, 24.08.2026: keep the immediate GLB warm-up needed for seamless switches, but stagger background decoding, stop warm-up copies after their upload frames, lower mobile render cost, and stop the render loop whenever the gallery is offscreen and idle.
- Mobile scene follow-up, 24.08.2026: bring the gallery table back toward the viewer with only a small side margin and seat the live board slightly lower over it. Keep the rotating-platter video on mobile, but load it on demand and crop it to the same table scale so it cannot reintroduce a full-screen jump. The four-board panorama may lose a little empty edge, but all boards stay complete and its soft upper/lower dissolve remains visible.
- Mobile video compositing correction, later 24.08.2026: never reveal a second translucent table during a board transition. The accepted still remains the only full stand; the video contributes only its fully opaque central rotating platter, matched to the still's exact centre and diameter.
- Mobile scene correction, latest 24.08.2026: the accepted still and transition video live inside the same normalized 1586:992 `.board-selector-media` frame. The clip uses its measured internal top offset of 6.81% inside that frame, so the same platter scanline stays aligned across Safari, Chrome and changing aspect ratios; do not return to independent viewport offsets.
- Mobile menu motion correction, latest 24.08.2026: the middle hamburger line must disappear at the start of the opening motion, before the outer lines finish forming the close icon. On close it may return slightly later, after the diagonals have begun separating, so it never flashes through the centre of the X.
- Editorial correction, latest 24.08.2026: keep only client-sourced factual sections and remove obvious repetitions. The repeated «Подход» band, the invented five-stage process and FAQ items that merely restated services, reverse engineering or deliverables are rejected. Keep the two operational FAQ items about ordering one part of the work and what to send initially.
- Navigation correction, latest 24.08.2026: the last item in the mobile menu is «Оставить заявку» linking to `#contact`, not the company email address.
- Desktop gallery framing, latest 24.08.2026: crop 44px of empty scene from the top by moving the complete selector stage beneath the section clip. Never rescale or independently offset the table, transition video or live board to achieve this crop.
- Mobile composition correction, latest 24.08.2026: the hero may use the existing theme-matched board scene as a subdued static background behind the copy, never as mobile WebGL or a separate card. In reverse engineering, the accepted whole scene must bleed to the viewport and dissolve between the description and steps; an inset rectangular image tile is rejected.
- Gallery interaction correction, later 24.08.2026: when a visitor has manually rotated a board, the outgoing board must start the requested turn immediately from that exact pose. Do not visibly return it or the camera to home first; reset only at the edge-on model swap under blur, then let the incoming board complete the turn and land in the canonical presentation pose.
- Gallery screen-direction correction, later 24.08.2026: the arrow direction is screen-relative, not tied to the model's or room's world axes. Capture camera-local vertical as the outgoing spin axis at the click, recapture it after the hidden canonical camera reset, and preserve the same direction sign so an extreme top/side inspection pose cannot make the two halves of one transition appear to reverse.
- Gallery board correction, latest 24.08.2026: `CAN-to-PWM` uses `baseRotation: 0.39` so it lands front-facing rather than slightly sideways. The 01/07 wrap keeps both exact model scales and uses only a brief stronger blur/opacity bridge at the edge-on swap; never soften that size difference by zooming either board.
- Initial loader correction, latest 24.08.2026: show the restrained SKS wordmark loader for at least 620 ms and wait for the current logo, hero and four first-screen font faces, capped at 3.6 s on mobile and 2.2 s elsewhere. Do not wait for every page font, GLB, section scene or the contact panorama before revealing the page. Mobile hero copy fades through blur without translating, so it never appears at one position and then jumps to another.
- Mobile 3D loading correction, latest 24.08.2026: never download or parse all seven GLBs concurrently on a phone. Prioritise the active flight-controller model, skip automatic alternate-theme image warming, omit the seven-model GPU warm-up on compact viewports, then warm only the next and previous boards after the active model reports ready. Desktop may still warm the complete set for seamless wraps.
- Mobile hero vertical correction, latest 24.08.2026: keep the title, description and CTA as one end-aligned group in the lower part of the first screen with the existing safe bottom inset. Do not solve the former empty lower strip by resizing the approved type or moving the background independently.
- Board copy verification, latest 24.08.2026: technical descriptions come from `скс.docx` and the client screenshot `client-materials/2026-08-10/Picsew_20260810205531.PDF`, cross-checked against `MATERIALS.md`. Keep the gallery title terse and expose the matching description only through the adjacent accessible `i` control; never add specifications, use cases or performance figures that are absent from those sources.
- Desktop scene and board-info correction, latest 24.08.2026: never crop or overlap the lamp out of the reverse-engineering photograph. Remove only the desktop services section's bottom padding and place the complete accepted media frame directly after the final service rule, preserving the entire upper lamp and light field. Leave the mobile calibration unchanged. The board description opens below its title on desktop as well as mobile so it cannot cover the `Разработки` heading; its card follows the theme with a dark surface in dark mode and a quiet light-neutral surface in light mode.
- Mobile finishing correction, latest 24.08.2026: the hero uses the existing empty laboratory stage plus the same transparent flight-controller render as desktop, composed statically without WebGL. Keep the complete stand inside the frame and scale the board to read clearly on its mounting plate, slightly above the copy. The accepted four-board panorama may crop only its empty background on mobile; every board stays complete and the lower edge dissolves into the request section so the strip reads as a transition rather than a separate section. Project-form focus is drawn on the field border without an outside gap, and the mobile-menu CTA targets `#project-form` rather than the top of the contact section.
- Mobile hero alignment correction, latest 24.08.2026: enlarge and raise the photographed mounting plate slightly relative to the transparent PCB, lift its dark exposure enough to read the plate edges, and counter-rotate the baked angled PCB render by the same `-16.5deg` used on desktop so it sits straight on the fixture. Keep the static two-layer composition and do not introduce mobile WebGL. In dark mode keep the independent PCB restrained enough that it does not glow against the fixture, but do not bury it beneath the atmospheric veil. On the light theme the PCB sits above the pale veil with the same crisp contrast and grounded two-stage shadow as desktop; never wash it into a translucent grey silhouette.
- Light-theme accent correction, latest 24.08.2026: emphasized words in hero and section headings stay black (`--fg`) on the light theme. The yellow-gold text treatment belongs only to the dark theme; the light-theme experiment was rejected.
- Mobile hero theme parity, latest 24.08.2026: the complete stand and its independent PCB use the same raised position in dark and light themes so switching themes never moves the composition. Only the light photograph gets the lower alpha dissolve into `--bg`; never expose a hard horizontal image edge in the light hero.
- Compact in-app browser correction, 25.08.2026: keep the accepted 393 x 852 mobile hero unchanged. On short or narrow visual viewports such as Telegram WebView, scale and raise the complete stage + PCB composite together so the board never changes size independently from its photographed mounting plate. Keep the PCB fully opaque and physically above the heading; apply the fog-like dissolve only to the quiet laboratory/table photograph behind it. Never mask the PCB itself or expose a diagonal cut across it. Cover the intermediate 399-403 x 720-768 range as well as the shortest screens; apply the same geometry in both themes and retain only the existing theme-specific exposure and shadows.
- Social preview correction, 25.08.2026: keep the 1200 x 630 OG card consistent with the live hero and use the same exact flight-controller render. The complete mounting plate and PCB sit farther right so the factual headline never crosses the hardware; counter-rotate the baked board angle until its presentation reads upright, keep it clearly exposed against the dark plate, and use the site's self-hosted Unbounded and Golos Text fonts.
- Favicon correction, 25.08.2026: use the single exact `S` circuit mark rather than squeezing the complete `SKS` wordmark into 16-32 px. Its main body must use the same yellow-gold gradient as `sks-logo-for-dark-bg.svg`, with the copper trace inside; keep every favicon and touch icon transparent, frameless and tightly cropped vertically so browser chrome cannot read as black bars above and below the mark. Version favicon URLs when replacing the raster set because browsers cache these files aggressively.
- Light mobile gallery boundary, latest 24.08.2026: the `Разработки` laboratory photograph must fade in from the section's light `--bg` at its upper edge. Use the existing selector vignette so the board, title and controls stay crisp, preserve the accepted lower dissolve, and do not change the dark theme.
- Mobile photographic boundary correction, latest 24.08.2026: in the light `Разработки` scene mask the top of `.board-selector-media` itself so Safari cannot reveal its rectangular edge; keep the live PCB and controls outside that mask. In the four-board contact strip, crop more of the empty upper laboratory area in both themes, keep all four boards fully opaque and complete, and begin the lower dissolve below their bases so no fog sits on the boards.
- Light gallery information control, latest 24.08.2026: the circular `i` control in the light `Разработки` theme uses a matte white surface, dark readable glyph and restrained warm border/shadow. Keep the dark-theme control unchanged and preserve the existing accessible label, expanded state and focus ring.
- Lower mobile boundary, later 24.08.2026: the four-board strip dissolves upward into true near-black, never blue-grey, and sits closer to the contact heading. Keep the lit reverse-engineering scene complete but soften its rectangular edges into the surrounding black.
- Visual feedback, later 21.08.2026: the light hero must be crisp rather than foggy; keep contrast in the laboratory backdrop and the board, and use a clear copper accent instead of muddy brown. Hero copy should read naturally and must not lead with the legal entity name.
- In `privacy.html`, the back action belongs on the left and the wordmark on the right. In `Разработки`, board changes should feel like a camera move across one laboratory stand while preserving exact, uncropped board renders.
- Visual feedback, later 21.08.2026: `Разработки` uses one short heading only. The live board sits over the sharp foreground octagonal stand; three matching positions follow a compact circular indexing path in the optically blurred background. The room stays fixed while stands/boards glide around the shallow arc; do not rotate the whole laboratory or build a visible giant turntable. Navigation uses clear standalone chevrons with no attached lines, and the bottom caption is reduced to the title and count so it does not cover the scene.
- The strip after FAQ is one continuous laboratory panorama with four identical cradles. Each cradle receives one exact client 3D model, front-facing and normalized; do not bake multiple boards into the photograph, tilt them randomly, add suspension cables, or combine several modules on one stand.
- The reverse-engineering photograph is a full-width scene behind the section, with the central fixture between the left description and right steps. Use strong edge shading for copy contrast and avoid presenting it as a separate framed card.
- The reverse fixture is deliberately much brighter than its room: a warm-neutral overhead inspection light isolates the PCB while the laboratory stays dark. The overhead light and its pool stay completely static; never animate the beam, glow, or floor illumination, and never turn it into a visible blue beam, neon effect, or game-like animation.
- For reverse engineering, use one existing complete scene as the background. Do not cut a lamp from another image, overlay a separate fixture, or generate a replacement scene. The accepted source is `exec-1321f294-cf35-40d1-a042-0c07e7c5eef7.png`; `exec-8db31dcf-8a9f-4237-9c7b-01ec033c06bf.png` is rejected because its lamp is oversized. Do not add a visible paper texture; preserve only the source photograph's own fine material detail.
- The contact panorama uses the complete `contact-panorama-boards-final.png` scene. Shift the photograph upward enough to keep the cradle bases visible, and fade it softly from the FAQ background into the contact-section background without a hard border.
- Final transition direction, 21.08.2026: keep the contact panorama compact above the boards, start the upper fade inside the empty laboratory area, and dissolve the lower table directly into the contact section's neutral dark grey. Do not introduce a blue band, a white hairline, or a radial glow from the form on the right.
- In `Разработки`, the laboratory photograph must begin at the section boundary with the heading inside the scene, not below a separate dark slab. Carousel motion is built from independent exact board renders and genuine-alpha turntable layers moving together at a constant camera scale; never stretch, morph, or bake the boards into generated video. Reject any asset whose checkerboard is baked into RGB pixels.
- Boundary feedback, later 21.08.2026: Process must flow into FAQ without doubled rules or a large empty slab. Reverse engineering must dissolve into `Разработки` through one shared near-black photographic overlap, never through a visible band. Light-theme scene variants must preserve the accepted boards, fixtures, framing and geometry exactly; only the room lighting and neutral material palette may change.
- Light reverse scene feedback, later 21.08.2026: retain a local off-camera inspection-light pool on the PCB while the rest of the light laboratory recedes into a nearly invisible neutral-grey field. The light source itself must not be visible; never turn the whole room into an evenly exposed product photo.
- Light reverse fixture feedback, later 21.08.2026: in the light theme the mounting fixture behind the exact black PCB is matte white or light neutral. Keep the PCB itself black and unchanged; do not leave the fixture as a heavy black slab.
- Light panorama and gallery feedback, 22.08.2026: all PCB mounting cradles and indexing stands are matte white or light neutral in the light theme while the exact boards stay black. `Разработки` gets a dedicated complete light laboratory plate and must show the uncropped scene without the former translate-and-scale zoom; the live 3D board remains a separate freely rotatable layer.
- Light gallery correction, later 22.08.2026: the light laboratory plate has exactly one central white foreground stand, matching the dark composition. Remove every rear stand, motion-path arc, ghost trail and table artefact; distant equipment stays strongly defocused and low-contrast so it never competes with the live board.
- Gallery motion feedback, 22.08.2026: prototype the right-arrow transition in Blender before trying Seedance or another video model. Keep one locked camera and constant scale; all four stands exchange positions along the same shallow carousel path, and every exact GLB board stays rigidly attached to its own stand with no morphing, stretching or zoom.
- Gallery motion correction, later 22.08.2026: the Blender carousel prototype is rejected. Return to one central foreground stand and keep the board as the live, fully opaque, freely rotatable GLB. On either arrow, the current board exits vertically upward and the next board descends from above onto the same stand; no video replacement, multiple stands, lateral carousel, scene transparency, model deformation or loss of orbit controls. Keep the title and count visibly above the lower scene fade.
- Gallery speed-ramp exploration, later 22.08.2026: if the existing rotating-platter clip is tested, reveal only its central foreground platter and match that platter exactly to the accepted still at a locked camera scale. Exclude every side/rear circle from the mask. The independent exact GLB turns in the same screen-space direction through one uninterrupted speed ramp, swaps at peak motion blur, and never pauses, reverses, stretches or loses orbit controls after landing.
- Gallery orientation correction, later 23.08.2026: do not snap the current board or camera to the canonical pose when an arrow is pressed. Preserve its actual pose at the click, blend the camera return into the fast ramp so the visible turn stays horizontal in the arrow direction regardless of the inspection angle, reset at the model hand-off under peak blur, and let the incoming exact GLB complete the remaining turn so it lands face-on.

## Logo lab decisions

- Current client-comparison set: `59`, `01.4A`, `28`, `33`, `40`, `61`. Remove `01.3B` and `36` from the visible switcher.
- Show shortlisted marks directly on the Carbon / Champagne and Light Laboratory environments with no ivory preview card or baked checkerboard.
- Preserve each generated mark exactly; for the local comparison page, remove only its connected light canvas and keep the board, lettering, traces and contacts unchanged.
- Use a larger `contain` logo treatment in the upper-left brand lockup so horizontal and square marks remain readable.
- Keep the header mark fully inside the header boundary. Do not add CSS glow or drop-shadow to either the header mark or the large comparison specimen, and keep the specimen clear of the caption and switcher.
- The next focused wordmark is `59`: preserve both S letterforms from `56`, round the K stroke ends and inset copper route like `57`, but keep one continuous clean K junction like `58` with no touching-path gap.
- In the logo-lab switcher, replace `01` with `59`, remove `11`, and retain the other comparison candidates.
- Candidate `60` removed the two white-filled interior via dots from `36`, but its remaining horizontal gold bridges made the letters look connected and distorted the K; keep it only as a counted rejected attempt.
- Candidate `61` removes those horizontal bridges entirely, restores clean dark gaps between the letters, and uses a conventional standalone K while preserving the four large corner mounting holes and the rest of the PCB silhouette.
- Candidate `62` is the first graphite/copper light-background colorway of `59`, but it is rejected because the generator baked a checkerboard into the bitmap instead of returning alpha.
- Candidate `63` preserves the graphite/copper `62` wordmark and supplies genuine transparency. Use it automatically as the light-theme asset for visible candidate `59`; keep the champagne/copper `59` asset on the dark theme.
- Candidates `64–68` are champagne/copper palette studies for visible marks `01.4A`, `28`, `33`, `40`, and `61` respectively. Preserve their geometry and use these recolors as the default comparison palette.
- Keep a compact `Шампань / Жёлтый` control in the logo-lab switcher. Default to `Шампань`; `Жёлтый` restores the earlier assets for direct comparison. Candidate `59` keeps its own dark/light theme pair and is unchanged by this palette control.
- The raw palette-study canvases are passed through the existing neutral-backdrop removal before display so only the logo/board remains on the physical stage. Do not show checkerboards or ivory source fields in the logo-lab.
- In the logo-lab hero, keep the physical mounting plate centered inside the right-hand preview area and center every logo on that plate. Use the dedicated centered dark/light stage crops so neither the plate nor the logo drifts toward the right edge; verify both horizontal and square marks at desktop and mobile widths.
- The provisional client-facing set is `59`, `01.4A`, `28`, `33`, `40`, and cleaned `61`; this is still a comparison set, not a final approval.
